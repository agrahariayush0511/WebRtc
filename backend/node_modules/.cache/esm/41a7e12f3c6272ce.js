let OtpService,HashService,userService,TokenService,UserDto;_3c6‍.x([["default",()=>_3c6‍.o]]);_3c6‍.w("../services",[["OtpService",["OtpService"],function(v){OtpService=v}],["HashService",["HashService"],function(v){HashService=v}]]);_3c6‍.w("../services/user-service",[["default",["userService"],function(v){userService=v}]]);_3c6‍.w("../services/token-service",[["default",["TokenService"],function(v){TokenService=v}]]);_3c6‍.w("../dtos/user-dto",[["default",["UserDto"],function(v){UserDto=v}]]);




const authController = {
    async sendOtp(req, res) {
        //* logic
        const {phone} = req.body
        if(!phone) {
            res.status(400).json({message: 'phone field is required!'})
        }

        const otp = await OtpService.generateOtp();

        //* Hash otp

        const ttl = 1000 * 60 * 5
        const expires = Date.now() + ttl
        const data = `${phone}.${otp}.${expires}`

        const hash = HashService.hashOtp(data);

        //* sending otp by sms-----------
        //* ------------------

        try{
            // await OtpService.sendBySms(phone, otp)
            return res.json({
                hash:`${hash}.${expires}`,
                phone,
                otp
            })
        }
        catch(err) {
            _3c6‍.g.console.log(err)
            res.status(500).json({message:'message sending failed'})
        }

        res.json({hash, otp})
    },

    async verifyOtp(req, res){
        //* logic

        const {otp, hash, phone} = req.body;

        if(!otp || !hash || !phone){
            res.status(500).json({message: 'All fields are required'})

        }

        const [hashedOtp, expires] = hash.split('.')
        if(Date.now() > +expires){
            res.status(400).json({message: 'OTP expired'})
        }
        const data = `${phone}.${otp}.${expires}`
        const isValid = OtpService.verifyOtp(hashedOtp, data)

        if(!isValid) {
            res.status(400).json({message: 'Invalid OTP'})
        }

        let user;

        try {
            user = await userService.findUser({phone: phone})
            if(!user) {
              user =  await userService.createUser({phone:phone})
              _3c6‍.g.console.log(user)
            }
        }
        catch(err) {
            _3c6‍.g.console.log(err)
            res.status(500).json({message: 'Db error'})
        }

        //* Token creating---------
        //* ---------------
        const {accessToken, refreshToken} = TokenService.generateTokens({
            _id: user._id,
            activated: false,
        })

        
        res.cookie('refreshToken', refreshToken, {
            maxAge: 1000 * 60 * 60 * 24 * 30,
            httpOnly: true,
        })

        const userDto = new UserDto(user)

         res.json({accessToken, userDto})

    }
}

_3c6‍.d(authController);