let tokenService;_45a‍.x([["default",()=>_45a‍.o]]);_45a‍.w("../services/token-service",[["default",["tokenService"],function(v){tokenService=v}]]);

const auth = async (req, res, next) => {
    try{
        const {accessToken} = req.cookies;
        if(!accessToken) {
            throw new Error();
        }

        const userData = await tokenService.verifyAccessToken(accessToken) 

        if(!userData) {
            throw new Error();
        }

        req.user = userData;

        next()
    }
    catch(err) {
        res.status(401).json({
            message: 'Invalid token'
        })
    }

}

_45a‍.d(auth);